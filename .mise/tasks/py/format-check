#!/usr/bin/env bash
#MISE description="Check Python formatting (ruff format --check, read-only)"
#MISE hide=true
#MISE raw=true

set -euo pipefail
# shellcheck disable=SC1091
source "$(dirname "$0")/../lib/py-env"

"${PY_RUNNER_ARR[@]}" ruff format --check "$PY_LINT_PATHS"
